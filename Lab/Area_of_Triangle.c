#include <stdio.h>
//Calculate Area of Triangle//

int main() {
    float h = 10.0;
    float l = 20.0;
    float area;

    area = (h * l) / 2;

    printf("Area of Triangle = %f", area);

    return 0;
}
